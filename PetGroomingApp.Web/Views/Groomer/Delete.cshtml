@model PetGroomingApp.Web.ViewModels.Groomer.GroomerDetailsViewModel
@{
    ViewData["Title"] = "Delete Groomer";
}

<partial name="_PageTitle" />

<section class="team_area pt-5 pb-5">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="p-4 border border-danger rounded shadow-sm bg-light text-center">
                    <h4 class="text-danger mb-4">Are you sure you want to delete <strong>@Model.Name</strong>?</h4>

                    <dl class="row">
                        <dt class="col-sm-3">Image:</dt>
                        <dd class="col-sm-9">
                            <img src="~/@Model.ImageUrl" alt="@Model.Name" class="img-thumbnail" style="max-width: 200px;">
                        </dd>

                        <dt class="col-sm-3">Name:</dt>
                        <dd class="col-sm-9">@Model.Name</dd>

                        <dt class="col-sm-3">Job Title:</dt>
                        <dd class="col-sm-9">@Model.JobTitle</dd>

                        <dt class="col-sm-3">Phone:</dt>
                        <dd class="col-sm-9">@Model.PhoneNumber</dd>

                        <dt class="col-sm-3">Description:</dt>
                        <dd class="col-sm-9">@Model.Description</dd>
                    </dl>

                    <form asp-action="DeleteConfirmed" method="post">
                        <input type="hidden" asp-for="Id" />
                        <div class="text-center mt-4">
                            <button type="submit" class="btn btn-danger fw-bold px-4 rounded-pill">
                                <i class="bi bi-trash"></i> Confirm Delete
                            </button>
                            <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary fw-semibold px-4 rounded-pill">
                                <i class="bi bi-x-circle"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>